NAME = libft.a

CLANG = gcc

CFLAGS = -Wall -Wextra -Werror

OBJ = 

all: $(NAME)

$(NAME): $(OBJ)
		ar rc	$(NAME) $(OBJ)

$(OBJ): %.o : %.c
		$(CLANG) $(CFLAGS) -c $*.c -o $@

clean:
		rm -rf $(OBJ)

fclean:
		rm -f $(NAME)

re:
		fclean all