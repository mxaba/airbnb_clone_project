<% layout('./layout') -%>
<section class="body">
  <div class="container">
    <div class="row text-center">
      <h1>Book unique places to stay and things to do</h1>
        <div class="btn-group">
          <form id="filterForm" method="get" class="form-inline" action="/listings_filter">
            <div class="form-group">
              <div class="col-xs-5 date">
                <div class="input-group input-append date" id="datePicker">
                  <input type="date" class="form-control" name="filter_date" require/>
                  <span class="input-group-addon add-on"><span class="glyphicon glyphicon-calendar"></span></span>
                </div>
              </div>
            </div>
          <div class="form-group">
            <div class="col-xs-5 col-xs-offset-3">
              <button type="submit" class="btn btn-label btn-info"><i class="fas fa-search"></i> Search availability</button>
            </div>
          </div>
        </form>
    <!--     <div class="container"> -->

          <h1>Houses available</h1>
          <% if (listings) { %>
            <% listings.forEach(function(listing, index) { %>
       <!--    <div class="table">
              <div class="column">
                  <a href=<%= "/bookings/new?id=" + listing.id %>><img src="<%= listing.image %>" style="width:100%; height:100%; border: 3px solid #cccccc;" ></a>
              </div>
          </div> -->

          <div class="table board">
                <main class="grid">
                
                    <a href=<%= "/bookings/new?id=" + listing.id %>><img src="<%= listing.image %>" style="width:100%; height:100%; border: 3px solid #cccccc;" ></a>
                  <div class="text">
            <h2><%= listing.name %></h2>
                      <h3><%= listing.description %></h3>
                    <p><i class="fas fa-map-marker-alt"></i>  <%= listing.location %></p>
                    <p>R <%= listing.price %> per night</p>
                   <button class="btn btn-primary btn-block"> <a href=<%= "/bookings/new?id=" + listing.id %>>Book a Space</a></button>
                  </div>
                </div>
              </main>
              <% });%>
              <% } %>
<!-- 
          <table class="table table-bordered" id="main-table">
            <thead>
              <tr>
                <th></th>
                <th>Name</th>
                <th>Price</th>
                <th>Available</th>
              </tr>
            </thead>
            <% if (listings) { %>
              <tbody>
                <% listings.forEach(function(listing, index) { %>
                <tr>
                  <th scope="row"><%= index + 1 %></th>
                  <td><a href=<%= "/bookings/new?id=" + listing.id %>><img src="<%= listing.image %>" style="width:200px; height:150px; border: 3px solid #cccccc;" ></a></td>
                  <td>R <%= listing.price %></td>
                  <td><%= new Date(listing.available).toDateString() %></td>
                </tr>
                <% });%>
              </tbody>
            <% } %>
          </table> -->
        </div>
      </div>

      </div>
    </div>
  </div>
</section>

<!-- <div class="container">

    <h1>Available Houses</h1>

    <table class="table table-bordered" id="main-table">
      <thead>
        <tr>
          <th></th>
          <th>Name</th>
          <th>Price</th>
          <th>Available</th>
        </tr>
      </thead>
      <% if (listings) { %>
        <tbody>
          <% listings.forEach(function(listing, index) { %>
          <tr>
            <th scope="row"><%= index + 1 %></th>
            <td><a href=<%= "/bookings/new?id=" + listing.id %>><img src="<%= listing.image %>" style="width:200px; height:150px; border: 3px solid #cccccc;" ></a></td>
            <td>R <%= listing.price %></td>
            <td><%= new Date(listing.available).toDateString() %></td>
          </tr>
          <% });%>
        </tbody>
      <% } %>
    </table>
  </div>
</div>
 -->